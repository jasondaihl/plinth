# Changesets

This directory holds [changesets](https://github.com/changesets/changesets) — small Markdown files that describe what changed in a PR and how it should bump package versions.

Run `pnpm changeset` to add a changeset for your current work. Run `pnpm version-packages` to apply pending changesets to package versions.
